// ─── Forgio Theme Config ──────────────────────────────────────────────────────
export const colors = {
  primaryBlue: '#1A56A0',    // Buttons, headers, icons
  secondaryBlue: '#2E75B6',  // Subheadings, links
  background: '#F3F4F6',     // Screen backgrounds
  surface: '#FFFFFF',        // Cards, modals
  textPrimary: '#111827',    // Main body text
  textSecondary: '#6B7280',  // Captions, placeholders
  success: '#16A34A',        // Success states, active
  warning: '#D97706',        // Warnings, pending
  error: '#DC2626',          // Errors, stopped status
  border: '#E5E7EB',         // Card borders, dividers
};

export const fontSizes = {
  xs: 12,    // Captions, timestamps
  sm: 14,    // Secondary labels
  base: 16,  // Body text (default)
  lg: 18,    // Card titles
  xl: 20,    // Section headings
  '2xl': 24, // Screen titles
  '3xl': 30, // Hero text
};
